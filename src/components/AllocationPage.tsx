export default function AllocationPage() {
  return (
    <div>
      <h2 style={{ fontFamily: 'var(--font-display)', marginBottom: '0.5rem' }}>
        📊 Update Allocation <span className="badge-custom badge-yellow" style={{ verticalAlign: 'middle', fontSize: '0.65rem' }}>US_05</span>
      </h2>
      <p style={{ color: 'var(--text-secondary)' }}>Allocation update with business rules — coming soon.</p>
    </div>
  );
}
