export default function AddMemberPage() {
  return (
    <div>
      <h2 style={{ fontFamily: 'var(--font-display)', marginBottom: '0.5rem' }}>
        ➕ Add Team Member <span className="badge-custom badge-blue" style={{ verticalAlign: 'middle', fontSize: '0.65rem' }}>US_01</span>
      </h2>
      <p style={{ color: 'var(--text-secondary)' }}>MemberFactory form with validation — coming soon.</p>
    </div>
  );
}
